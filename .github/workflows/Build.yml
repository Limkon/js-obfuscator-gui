name: Build Windows Exe

on:
  workflow_dispatch:

jobs:
  # ==========================================
  # 阶段 1: 下载与准备
  # ==========================================
  download-and-prepare:
    name: 📥 Download & Install Deps
    runs-on: windows-latest
    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # 这里的 Cache key 改一下，强制刷新缓存
      - name: Cache node_modules
        id: cache-node
        uses: actions/cache@v3
        with:
          path: node_modules
          key: ${{ runner.os }}-node-v2-${{ hashFiles('package.json') }}

      # 【核心修复 1】删除 lock 文件再安装，防止协议冲突
      - name: Install Dependencies
        if: steps.cache-node.outputs.cache-hit != 'true'
        run: |
          if (Test-Path package-lock.json) { Remove-Item package-lock.json }
          npm install

  # ==========================================
  # 阶段 2: 构建应用
  # ==========================================
  build-exe:
    name: 📦 Build Exe
    needs: download-and-prepare
    runs-on: windows-latest
    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # 对应上面的 key，加了 -v2
      - name: Restore node_modules
        uses: actions/cache@v3
        with:
          path: node_modules
          key: ${{ runner.os }}-node-v2-${{ hashFiles('package.json') }}

      # 【核心修复 2】这里也强制删除 lock 文件，确保重构原生依赖时不出错
      - name: Rebuild Native Deps
        run: |
          if (Test-Path package-lock.json) { Remove-Item package-lock.json }
          npm install

      - name: Run Build
        # 增加 GH_TOKEN 环境变量，防止 electron-builder 发布检查报错
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run dist -- --publish never

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Obfuscator-GUI
          path: dist/*.exe
