name: Build Windows Exe

# 仅手动触发
on:
  workflow_dispatch:

jobs:
  # ==========================================
  # 阶段 1: 下载与准备
  # ==========================================
  download-and-prepare:
    name: 📥 Download & Install Deps
    runs-on: windows-latest
    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # 缓存 node_modules (Key 增加 -v2 以强制刷新缓存)
      - name: Cache node_modules
        id: cache-node
        uses: actions/cache@v3
        with:
          path: node_modules
          key: ${{ runner.os }}-node-v2-${{ hashFiles('package.json') }}

      # 【核心修复】强制删除 lock 文件，防止 git/ssh 协议报错
      - name: Install Dependencies
        if: steps.cache-node.outputs.cache-hit != 'true'
        run: |
          if (Test-Path package-lock.json) { Remove-Item package-lock.json }
          npm install

  # ==========================================
  # 阶段 2: 构建应用
  # ==========================================
  build-exe:
    name: 📦 Build Exe
    needs: download-and-prepare
    runs-on: windows-latest
    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # 恢复上个阶段的依赖 (Key 必须与上面一致)
      - name: Restore node_modules
        uses: actions/cache@v3
        with:
          path: node_modules
          key: ${{ runner.os }}-node-v2-${{ hashFiles('package.json') }}

      # 【核心修复】再次确保无 lock 文件干扰
      - name: Rebuild Native Deps
        run: |
          if (Test-Path package-lock.json) { Remove-Item package-lock.json }
          npm install

      # 执行构建
      # 1. 注入 GITHUB_TOKEN 防止 electron-builder 报错
      # 2. 添加 --publish never 跳过发布步骤
      - name: Run Build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run dist -- --publish never

      # 上传构建好的 exe 文件
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Obfuscator-GUI
          path: dist/*.exe
